{
  "name": "notepadapp",
  "version": "1.0.0",
  "scripts": {
    "start": "node server/server.js",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "express": "^4.17.1"
    
  },
  "devDependencies": {
    
  },
  "author": "",
  "license": "ISC",
  "description": "A Notepad application with React frontend and Node.js backend"
}
